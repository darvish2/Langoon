&nbsp;
&emsp;




<br>
<br>
<a href="#" class="btn-sf-like">&emsp;&emsp;Vocabulary&emsp;&emsp;</a>
<br>
<br>
<% @posts.each do |post| %>
	<% if post.Vocabulary? %>
		<%= link_to "/languagenames/#{post.languagename_id}/posts/#{post.id}" do %>
			<button class="btn-gradient-3d">
				&emsp;&emsp;<%= post.title %>&emsp;&emsp;
			</button>

		<% end %>
	<% end %>
<% end %>



<br>
<br>
<a href="#" class="btn-sf-like">&emsp;&emsp;Syntax&emsp;&emsp;</a>
<br>
<br>
<% @posts.each do |post| %>
	<% if post.Syntax? %>
		<%= link_to "/languagenames/#{post.languagename_id}/posts/#{post.id}" do %>
			<button class="btn-gradient-3d">
				&emsp;&emsp;<%= post.title %>&emsp;&emsp;
			</button>

		<% end %>
	<% end %>
<% end %>



<br>
<br>
<a href="#" class="btn-sf-like">&emsp;&emsp;Practice&emsp;&emsp;</a>
<br>
<br>
<% @posts.each do |post| %>
	<% if post.Practice? %>
		<%= link_to "/languagenames/#{post.languagename_id}/posts/#{post.id}" do %>
			<button class="btn-gradient-3d">
				&emsp;&emsp;<%= post.title %>&emsp;&emsp;
			</button>

		<% end %>
	<% end %>
<% end %>



<br>
<br>
<a href="#" class="btn-sf-like">&emsp;&emsp;Others&emsp;&emsp;</a>
<br>
<br>
<% @posts.each do |post| %>
	<% if post.Others? %>
		<%= link_to "/languagenames/#{post.languagename_id}/posts/#{post.id}" do %>
			<button class="btn-gradient-3d">
				&emsp;&emsp;<%= post.title %>&emsp;&emsp;
			</button>

		<% end %>
	<% end %>
<% end %>
